pipeline {
    agent any
    stages {
        stage ('code') {
            steps {
                git 'https://github.com/ramyachetty/web-app.git'
            }
        }
        stage ('build') {
            steps {
                sh 'mvn clean package'
            }
        }
    }
}